apiVersion: v1
kind: Service
metadata:
  name: edge-computational-site
  labels:
      app: edge-computational-site
spec:
  selector:
    app: edge-computational-site
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      name: http
