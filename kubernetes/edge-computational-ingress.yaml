apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
    name: edge-computational-ingress
    labels: 
        app: edge-computational-site
spec:
    rules:
        - http:
            paths:
              - path: /comp
                pathType: Prefix
                backend:
                  service:
                    name: edge-computational-site
                    port: 
                      number: 80
    ingressClassName: nginx
