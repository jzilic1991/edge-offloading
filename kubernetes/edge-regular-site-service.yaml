apiVersion: v1
kind: Service
metadata:
  name: edge-regular-site
  labels:
      app: edge-regular-site
spec:
  selector:
    app: edge-regular-site
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      name: http
