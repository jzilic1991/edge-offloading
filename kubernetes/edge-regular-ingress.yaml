apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
    name: edge-regular-ingress
    labels: 
        app: edge-regular-site
spec:
    rules:
        - http:
            paths:
              - path: /regular
                pathType: Prefix
                backend:
                  service:
                    name: edge-regular-site
                    port: 
                      number: 80
    ingressClassName: nginx
